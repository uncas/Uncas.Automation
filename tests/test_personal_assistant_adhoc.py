def testIt():
	#from uncas_automation.assistant.Functions.findInfoInDocs import syncDocs # type: ignore
	#syncDocs()

	#from uncas_automation.assistant.Functions.findInfoInDocs import findInfoInDocs # type: ignore
	#info = findInfoInDocs("What are the plans for the office space?")
	#print(info)

	#from uncas_automation.assistant.Functions.getLatestNews import getNewsDetails, getLatestNews
	#news = getLatestNews()
	#print(news)
	#print(getNewsDetails(news[0]))

	#from uncas_automation.assistant.Functions.searchArxiv import searchArxiv
	#papers = searchArxiv({"query": "rag"})
	#print(papers)

	#from uncas_automation.assistant.Functions.getCurrentWeather import getCurrentWeather
	#weather = getCurrentWeather({"city": "Copenhagen", "countryCode": "DK"})
	#print("Weather: ", weather)

	#from uncas_automation.assistant.Functions.theMovieDb import getRecommendedMoviesThatIHaveAccessToWatch
	#movies = getRecommendedMoviesThatIHaveAccessToWatch()
	#for movie in movies:
	#	print(movie["rating"], movie["title"], movie["providers"])

	#from uncas_automation.Services.TheMovieDb.TmdbService import TmdbService
	#tmdbService = TmdbService()
	#tmdbService.rateMovieByTitle("Ashes of Time", 10)
	#tmdbService.rateMovieByTitle("The Godfather", 9)
	#print(tmdbService.getMyFavoriteMovies())
	#print(tmdbService.getMoviesIHaveWatched())

	#from uncas_automation.assistant.Functions.generateImage import generateImage, makePartOfImageTransparent
	#image = generateImage("A steampunk city with gear-driven machines, airships docked atop buildings, and streets lit by gas lamps, set in a vast canyon")
	#print(image)

	#from pathlib import Path
	#downloadsPath = str(Path.home() / "Downloads")
	#originalFilePath = downloadsPath + "/skriget.jpg"
	#newFilePath = downloadsPath + "/skriget_transparent.png"
	#xFraction, yFraction, widthFraction, heightFraction = 0.1, 0.1, 0.5, 0.5
	#makePartOfImageTransparent(originalFilePath, newFilePath, xFraction, yFraction, widthFraction, heightFraction)

	#from uncas_automation.assistant.Functions.createAssistant import createAndRunAssistant
	#createAndRunAssistant()

	#from uncas_automation.assistant.Functions.createJiraIssue import getMyJiraIssues, createJiraIssue
	#print(getMyJiraIssues()[0])
	#print(createJiraIssue({"summary": "TEST SUMMARY", "description": "TEST DESCRIPTION"}))

	#from uncas_automation.assistant.Functions.getCalendarEvents import getCalendarEvents
	#for item in getCalendarEvents():
	#	print(item) 

	#from uncas_automation.assistant.Functions.readWebPage import readWebPage
	#print(readWebPage("https://github.com/uncas/Uncas.Automation/blob/main/uncas_automation/assistant/Readme.md"))